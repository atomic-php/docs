{
  "name": "atomic-php-docs",
  "private": true,
  "version": "0.1.0",
  "description": "Documentation website for Atomic PHP (VitePress)",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.3.0"
  }
}

